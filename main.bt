// just a bunch of example code
import "std.bt"
import "lib.bt"

struct MyStruct {
    integer: i32, // 32 bit integer
    boolean: bool,
    pointer: Ptr<MyStruct>, // Ptrs can be NULL
    array: List<u8>, // dynamically allocated
}

func main(args: List<String>) {
    let rand = Random::seed(1234);

    if (args.contains("--help")) {
        let number = 42;
    } else if (rand.get()%100 == 0) {
        print("Wow, thats lucky");
    } else {
        
    }
    print("Hello World! 🗿");
}