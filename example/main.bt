// just an example program
import "example/std.bt"
import "example/lib.bt"

func main() {
	print("What is the password?");
	let x = input();
	
	print("You said: ", x);

	if (x == "hello") {
		print("Correct!");
	} else {
		print("Incorrect!");
	}
}